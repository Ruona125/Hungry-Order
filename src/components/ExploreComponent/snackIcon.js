import React from "react";
import { View, StyleSheet, ScrollView } from "react-native";
import { SnackImage } from "./snackImage.components";

export const SnackIcon = () => {
  return (
    <View>
      <SnackImage />
    </View>
  );
};
